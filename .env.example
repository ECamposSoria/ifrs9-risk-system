# Airflow Configuration
AIRFLOW_UID=50000
AIRFLOW_FERNET_KEY=zp8kV3K_7FXnW2XR_U3b0YQVTwFnCdQmHkp_dxKBQgw=
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow
AIRFLOW_WEBSERVER_PORT=8080

# Postgres Configuration
POSTGRES_PORT=5432

# Spark Configuration
SPARK_MASTER_PORT=7077
SPARK_MASTER_UI_PORT=8090
SPARK_WORKER_UI_PORT=8091

# Jupyter Configuration
JUPYTER_PORT=8888
JUPYTER_TOKEN=ifrs9

# PgAdmin Configuration (optional)
PGADMIN_PORT=5050
PGADMIN_DEFAULT_EMAIL=admin@admin.com
PGADMIN_DEFAULT_PASSWORD=admin

# GCP Configuration (for production)
GCP_PROJECT_ID=
GCP_BUCKET_NAME=
GCP_DATASET_ID=
GCP_REGION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=

# Data Processing Configuration
BATCH_SIZE=10000
MAX_WORKERS=4
LOG_LEVEL=INFO

# IFRS9 Parameters
DEFAULT_PD_RATE=0.02
DEFAULT_LGD_RATE=0.45
DEFAULT_EAD_PERCENTAGE=1.0
STAGING_CRITERIA_DAYS=30
STAGING_2_CRITERIA_DAYS=90

# ML Model Configuration
MODEL_VERSION=v1.0
MODEL_PATH=/app/models
CONFIDENCE_THRESHOLD=0.75

# Monitoring
ENABLE_MONITORING=true
METRICS_EXPORT_INTERVAL=60

# API Security (set in production)
API_JWT_SECRET=change-me
API_JWT_AUDIENCE=ifrs9-clients
# Disable auth ONLY for local dev
API_AUTH_DISABLED=false
TRUSTED_HOSTS=example.com
ENABLE_HSTS=false
CSP_POLICY=default-src 'self'; frame-ancestors 'none'
MAX_REQUEST_BYTES=2000000
RATE_LIMIT_WINDOW=60
RATE_LIMIT_MAX=120
